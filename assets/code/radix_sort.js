function radix_sort(A){
  const max = Math.max(...A)

  let m = 1
  while(m < max) {
    const buckets = Array.from({ length: 10 }, () => [])
    A.forEach( number => {
      digit = number
    })
    m *= 10
  }
}